use crate::{Class, ClassParam};

// Takes in classes, removes classes that overlap with events, and creates combinations
//pub async fn process_classes(mut classes: Vec<Vec<Class>>, params: Vec<ClassParam>) -> Result<Vec<Vec<Class>>, anyhow::Error> {

//}